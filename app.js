const links=[...document.querySelectorAll('[data-branch]')];const store=document.querySelector('#branch-store');const payment=['Pay $wholedonuts','https://cash.app/$wholedonuts'];const stores={tnc:payment,awd:payment};function sync(){const id=location.hash.slice(1);links.forEach(a=>a.classList.toggle('active',a.dataset.branch===id));if(stores[id]){store.textContent=stores[id][0]+' ↗';store.href=stores[id][1]}else{store.textContent='Choose a branch';store.href='#home'}}addEventListener('hashchange',sync);sync();