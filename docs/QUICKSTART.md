# Quickstart

```bash
npm install
npm run build
npm test
```

Try integration quickly:

```bash
node -e "const { renderWelcome } = require('@thewholedonuts/web-ui'); console.log(renderWelcome('I feel hopeful today'));"
```
