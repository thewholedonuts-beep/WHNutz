# Contributing

1. Create a branch from `main`.
2. Make focused changes in one or more workspaces.
3. Run `npm run build` and `npm test`.
4. Open a pull request with scope, rationale, and test notes.
